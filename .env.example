# VCF Migration Environment Configuration
# Copy this file to .env and configure as needed

# =============================================================================
# PRICING OPTIONS (Choose one)
# =============================================================================

# Option 1: Code Engine Proxy (RECOMMENDED)
# Deploy the pricing proxy to IBM Code Engine and set the URL here.
# This keeps your API key secure on the server side.
# See: functions/pricing-proxy/README.md for deployment instructions
VITE_PRICING_PROXY_URL=https://vcf-pricing-proxy.xxxx.us-south.codeengine.appdomain.cloud

# Option 2: Direct API Key (NOT RECOMMENDED for production)
# If you don't want to use the proxy, you can set the API key directly.
# WARNING: This exposes your API key in the browser!
# Get your API key from: https://cloud.ibm.com/iam/apikeys
# VITE_IBM_CLOUD_API_KEY=your-api-key-here

# =============================================================================
# PROFILES OPTIONS (Choose one)
# =============================================================================

# Option 1: Code Engine Proxy (RECOMMENDED)
# Deploy the profiles proxy to IBM Code Engine and set the URL here.
# This keeps your API key secure on the server side.
# See: functions/profiles-proxy/README.md for deployment instructions
VITE_PROFILES_PROXY_URL=https://vcf-profiles-proxy.xxxx.us-south.codeengine.appdomain.cloud

# Option 2: Direct API Key (NOT RECOMMENDED for production)
# The same API key as above can fetch live VPC instance profiles,
# bare metal profiles, and ROKS flavors directly from IBM Cloud APIs.
# WARNING: This exposes your API key in the browser!
#
# APIs used:
# - VPC VSI Profiles: GET /v1/instance/profiles
# - VPC Bare Metal:   GET /v1/bare_metal_server/profiles
# - ROKS Flavors:     GET /global/v2/getFlavors
#
# If neither proxy nor API key is set, the app uses static profile data.
#
# REQUIRED IAM PERMISSIONS for API Key:
# -------------------------------------
# 1. VPC Infrastructure Services - Viewer role
# 2. Kubernetes Service - Viewer role (for ROKS machine types)

# =============================================================================
# NOTES
# =============================================================================
# - If neither is set, the app will use static fallback pricing and profile data
# - The proxy is preferred for pricing because it:
#   1. Keeps credentials server-side (secure)
#   2. Caches responses to reduce API calls
#   3. Handles CORS issues automatically
#   4. Falls back gracefully if API is unavailable
